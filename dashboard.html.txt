{% extends 'base.html' %}
{% block content %}
<h1>Metrics Overview</h1>
<div class="grid">
    <div class="card">
        <h3>Active Children</h3>
        <p style="font-size: 2rem; font-weight: bold;">{{ child_count }}</p>
    </div>
    <div class="card">
        <h3>Total Financial Ledger</h3>
        <p style="font-size: 2rem; font-weight: bold;">${{ "%.2f"|format(total_donations) }}</p>
    </div>
    <div class="card">
        <h3>Low-Stock Items</h3>
        <p style="font-size: 2rem; font-weight: bold;">{{ low_stock|length }}</p>
    </div>
    <div class="card">
        <h3>Pending Adoptions</h3>
        <p style="font-size: 2rem; font-weight: bold;">{{ pending_adoptions }}</p>
    </div>
</div>

{% if current_user.role == 'Admin' %}
<div class="card" style="margin-top: 2rem;">
    <h3>Privacy Controls</h3>
    <form action="{{ url_for('privacy_toggle') }}" method="POST">
        <button type="submit" class="btn btn-danger">
            Toggle Privacy Masking (Current: {{ "ENABLED" if masking_enabled else "DISABLED" }})
        </button>
    </form>
</div>
{% endif %}
{% endblock %}